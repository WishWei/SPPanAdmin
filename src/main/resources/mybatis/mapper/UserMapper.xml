<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.sppan.base.dao.mapper.UserMapper">
    
    <resultMap id="user" type="net.sppan.base.entity.User"/>

    <sql id="columns">id,address,birthday,create_time,delete_status,description,email,
                locked,nick_name,password,sex,telephone,update_time,user_name</sql>

    <select id="findUserById" resultMap="user">
        select <include refid="columns"/>
        from tb_user
        where id = #{id}
    </select>
</mapper>